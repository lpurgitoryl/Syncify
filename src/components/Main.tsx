function Main() {
  return (
    <div id="main-container">
      {/* 
      {this.props.hasAnalysis ? (
        <Visuals
          energy={this.props.energy}
          song_id={this.props.nowPlaying.song_id}
          songAnalysis={this.props.songAnalysis}
          nowPlaying={this.props.nowPlaying}
        />
      ) : (
        console.log("no visual")
      )} */}
      {/* <Player
      getSong_id={this.getSong_id}
      getNowPlaying={this.props.getNowPlaying}
      nowPlaying={this.props.nowPlaying}
      /> */}
    </div>
  );
}

export default Main;
